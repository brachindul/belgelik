import 'package:belgelik/main.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Belgelik app can be constructed', () {
    expect(const BelgelikApp(), isA<BelgelikApp>());
  });
}
